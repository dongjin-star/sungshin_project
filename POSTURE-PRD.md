# POSTURE — 제품 요구사항 명세서 (PRD)

> **문서 버전** v1.0
> **작성일** 2026-08-26
> **상태** 개발 착수 전 · 선행 조건 2건 미해결 (§0 참조)
> **서비스명** `POSTURE`는 **코드네임(placeholder)**이며 확정된 이름이 아니다. §16.1 참조.

---

## 0. 개발 착수 전 반드시 해결해야 할 선행 조건

이 두 가지가 해결되지 않으면 **아래 모든 내용이 무효가 될 수 있다.** Phase 1 착수 전에 순서대로 처리한다.

| # | 항목 | 내용 | 미해결 시 영향 |
|---|---|---|---|
| **P-01** | **토스증권 약관 확인** | `client_id`는 개인 토스증권 계좌로 발급된다. 이 credential로 **불특정 다수에게 시세를 재배포하는 공개 서비스**를 운영하는 것이 이용약관상 허용되는지 공식 문서에 명시가 없다. 토스증권에 직접 문의해 서면 확인을 받는다. | 🔴 **데이터 소스 자체를 교체해야 함.** 대안: 한국투자증권 KIS Developers |
| **P-02** | **고정 아웃바운드 IP 확보** | 토스증권 API는 WTS에 등록한 허용 IP 외의 호출을 403으로 차단한다. Vercel 서버리스는 아웃바운드 IP가 고정되지 않는다. | 🔴 **배포 인프라 변경 필요.** §10 참조 |

> **P-01을 가장 먼저 처리한다.** 아키텍처 작업(P-02)은 데이터 소스가 확정된 뒤에 의미가 있다.

---

## 1. Product Overview

### 1.1 한 줄 정의

**차트를 읽지 못하는 사람에게, 지금 이 종목이 최근 가격 분포에서 어디쯤에 있고 단기·장기 흐름이 어떻게 교차했는지를 사람 신체 은유로 보여주는 정보형 웹앱.**

### 1.2 해결하려는 문제

증권 앱을 열면 캔들 차트, 이동평균선 5개, 거래량 바, 보조지표 탭이 동시에 쏟아진다. 주식을 실제로 보유하고 있으면서도 이 화면에서 **아무 정보도 얻지 못하는 사용자층**이 두텁게 존재한다.

이들이 실제로 알고 싶은 것은 대체로 두 가지뿐이다.

1. **지금 이 가격이 최근 기준으로 높은 편인가 낮은 편인가**
2. **최근 흐름이 위로 꺾였나 아래로 꺾였나**

기존 앱은 이 두 질문에 답하기 위해 사용자가 차트 읽는 법을 먼저 배울 것을 요구한다. POSTURE는 순서를 뒤집는다. **답을 먼저 주고, 원한다면 근거를 펼쳐 보게 한다.**

### 1.3 핵심 가치

| 가치 | 구체적 의미 |
|---|---|
| **번역** | 퍼센타일·이동평균 교차 → 신체 부위·일상 언어 |
| **정직** | 계산 근거·기준 기간·데이터 한계를 숨기지 않고 화면에 상시 표시 |
| **비추천** | 매수·매도 판단을 대신하지 않는다. 위치를 재는 눈금자이지 평가자가 아니다 |
| **단일 은유** | 하나의 캐릭터로 모든 종목을 같은 방식으로 비교할 수 있다 |

### 1.4 타깃 사용자

- **주 타깃** — 주식 계좌가 있고 실제로 종목을 보유하지만, 차트에서 판단 근거를 얻지 못하는 20~40대
- **부 타깃** — 투자를 막 시작해 종목을 고르는 단계이며, 기존 증권 앱의 정보 밀도에 압도된 사용자
- **비타깃** — 전문 트레이더, 데이 트레이더, 보조지표를 능동적으로 조합하는 사용자. **이들을 위한 기능은 의도적으로 넣지 않는다.**

### 1.5 제품 원칙 (Product Principles)

이 다섯 가지는 개별 기능 결정보다 상위에 있으며, 충돌 시 항상 우선한다.

| ID | 원칙 | 실무적 함의 |
|---|---|---|
| **PP-01** | **캐릭터는 눈금자이지 평가자가 아니다** | 위치에 따라 캐릭터의 **표정·색상·자세를 바꾸지 않는다.** 무릎에서 웃고 머리에서 찡그리면 그 자체가 매매 권유다 |
| **PP-02** | **모든 문구는 사실 진술이어야 한다** | "32% 지점"(해석) 대신 "120거래일 중 32%의 날보다 높음"(사실). §13 금지 표현 목록 준수 |
| **PP-03** | **기준을 숨기지 않는다** | 기간·계산식·데이터 시각을 화면에서 항상 확인 가능 |
| **PP-04** | **자연어 생성(LLM)을 쓰지 않는다** | 설명 문구는 100% 결정론적 템플릿. §7.6 참조 |
| **PP-05** | **위치와 추세를 조합해 결론을 만들지 않는다** | 두 정보를 나란히 두되, "무릎 + 골든크로스 = 좋은 신호" 같은 종합 판정을 앱이 대신 내리지 않는다 |

> **PP-05의 배경.** 초기 기획안에는 `무릎 21% + 골든크로스`를 하나의 카드로 묶어 보여주는 Case A가 있었다. 매수하라고 쓰지 않아도 **낮은 위치 + 긍정적 추세 전환의 조합은 매수 신호로 읽힌다.** 문구를 순화하는 것으로는 해결되지 않으므로, **화면 자체를 분리**하는 것으로 대응한다(§5.3, §5.4).

---

## 2. User Persona

### 2.1 페르소나 A — 보유 중인데 판단이 안 서는 사람 (주 타깃)

| 항목 | 내용 |
|---|---|
| 프로필 | 32세, 직장인. 증권 계좌 3년차. 국내 5종목·미국 3종목 보유 |
| 행동 | 아침에 앱을 켜서 등락률만 확인하고 닫는다. 차트 탭은 거의 들어가지 않는다 |
| **Pain Point 1** | "지금 물린 건지 아직 괜찮은 건지 모르겠다." 등락률은 어제 대비일 뿐, **기준점이 없다** |
| **Pain Point 2** | 이동평균선이 뭔지는 알지만, 지금 이 종목의 MA20이 71,200원이라는 숫자가 **무엇을 의미하는지 모른다** |
| **Pain Point 3** | 유튜브에서 "골든크로스 떴다"고 하는데, **내 종목에도 떴는지 확인할 방법을 모른다** |
| 성공 시나리오 | 앱을 켜고 5초 안에 "내 8종목이 각각 어디쯤인지" 한 화면에서 파악 |

### 2.2 페르소나 B — 종목을 고르는 중인 초심자

| 항목 | 내용 |
|---|---|
| 프로필 | 26세, 사회초년생. 적립식 투자 시작 3개월차 |
| 행동 | 뉴스나 커뮤니티에서 종목명을 보고 검색해본다 |
| **Pain Point 1** | 종목을 검색해도 **비싼 건지 싼 건지 감이 없다**. PER 같은 지표는 검색해봐도 이해가 안 된다 |
| **Pain Point 2** | 증권 앱 종목 화면의 정보 밀도가 너무 높아서 **어디를 봐야 할지 모른다** |
| **Pain Point 3** | "지금 들어가도 되나"를 물어볼 곳이 커뮤니티밖에 없고, 거기 답은 신뢰가 안 간다 |
| 성공 시나리오 | 종목명 검색 → 캐릭터 화면 1개 → "아 지금은 최근 1년 중에서는 높은 편이구나" 정도의 감각 획득 |

### 2.3 페르소나 C — 관심종목 관리자 (부 타깃)

| 항목 | 내용 |
|---|---|
| 프로필 | 41세, 자영업. 10~15개 종목을 관심목록으로 관리 |
| 행동 | 주 2~3회 관심종목 전체를 훑는다 |
| **Pain Point 1** | 증권 앱 관심종목은 **등락률만 보여줘서 종목 간 비교가 안 된다.** 삼성전자 -1%와 소형주 -1%는 의미가 다르다 |
| **Pain Point 2** | 15개 종목을 하나씩 눌러서 차트를 봐야 하는데 **시간이 오래 걸린다** |
| 성공 시나리오 | 관심종목 리스트에서 위치·추세를 **동일한 척도로 나란히 비교** |

---

## 3. Core User Flow

### 3.1 주 플로우

```
[앱 진입]
    │
    ├─ 관심종목 있음 ──▶ [화면 5: 관심종목 리스트]  ← 기본 진입점
    │                        │ 종목 탭
    │                        ▼
    └─ 관심종목 없음 ──▶ [화면 1: 종목 검색]
                             │ 검색어 입력 → 결과 선택
                             ▼
                    ┌────────────────────────┐
                    │  [화면 2: 위치]         │  ← 종목 상세 기본 탭
                    │   캐릭터 + 퍼센타일     │
                    │   기간 토글 60/120/250  │
                    └───────┬────────────────┘
                            │ 탭 전환 (스와이프 가능)
                            ▼
                    ┌────────────────────────┐
                    │  [화면 3: 흐름]         │
                    │   배열 상태 + 교차 배지  │
                    └───────┬────────────────┘
                            │ 탭 전환
                            ▼
                    ┌────────────────────────┐
                    │  [화면 4: 쉬운 설명]    │
                    │   템플릿 문장 3~4줄     │
                    └───────┬────────────────┘
                            │
                            ▼
                    [관심종목 추가] ──▶ 화면 5로 복귀
```

### 3.2 플로우 설계 근거

**① 관심종목을 기본 진입점으로 둔다.**
페르소나 A·C의 재방문 동기는 "내 종목 확인"이다. 매번 검색부터 시작하게 하면 재방문 비용이 커진다. 최초 방문자만 검색 화면으로 보낸다.

**② 화면 2·3·4를 탭으로 분리한다 (스크롤 아님).**
PP-05에 따라 **위치와 추세를 한 화면에 조합해 보여주지 않는다.** 세로 스크롤로 이어 붙이면 사용자는 자연히 두 정보를 하나의 결론으로 합성한다. 탭으로 분리하면 각각을 독립된 사실로 인식한다.

**③ 종목 상세의 기본 탭은 화면 2(위치)다.**
이 앱의 정체성이 위치 은유이므로, 진입 시 첫 화면이 캐릭터여야 한다.

### 3.3 보조 플로우

| 플로우 | 경로 |
|---|---|
| 기간 변경 | 화면 2 → 토글(60/120/250) → **API 호출 없이 즉시 재계산** (§10.4) |
| 계산 근거 확인 | 화면 2·3 → `ⓘ` → 바텀시트(계산식 + 기준 기간 + 데이터 시각) |
| 관심종목 삭제 | 화면 5 → 스와이프 또는 편집 모드 |
| 검색 재진입 | 화면 5 상단 검색 아이콘 → 화면 1 |

---

## 4. MVP 기능 정의

### 4.1 Must Have — Phase 1

| ID | 기능 | 상세 |
|---|---|---|
| **F-SEARCH-01** | 종목 검색 | 한글명·영문명·티커·6자리 코드로 검색. KR + US |
| **F-SEARCH-02** | 자체 검색 인덱스 | `/stocks/all`로 종목 마스터를 일 1회 동기화해 자체 인덱스 구축 (토스 API에 이름 검색 엔드포인트 없음) |
| **F-SEARCH-03** | 초성 검색 | "ㅅㅅㅈㅈ" → 삼성전자. 한국 사용자에게 사실상 필수 |
| **F-SEARCH-04** | 최근 검색어 | 로컬 저장, 최대 10건 |
| **F-POS-01** | 퍼센타일 위치 계산 | §7.2 공식. 기본 120거래일 |
| **F-POS-02** | 6구간 신체 매핑 | 발/무릎/허리/가슴/어깨/머리. §7.3 경계값 |
| **F-POS-03** | 캐릭터 시각화 | 위치 마커 + 구간 라벨. **표정·색상 불변 (PP-01)** |
| **F-POS-04** | 기간 토글 | 60 / 120 / 250거래일. 기본값 120. 선택값 로컬 저장 |
| **F-POS-05** | 경계 히스테리시스 | 구간 전환은 경계 ±2%p를 넘겼을 때만 반영 |
| **F-POS-06** | 부가 수치 | 현재가, 전일 대비 등락률, 기간 최고가·최저가, 데이터 기준 시각 |
| **F-TREND-01** | MA20 / MA60 계산 | 단순이동평균(SMA), 수정주가 종가 기준 |
| **F-TREND-02** | 배열 상태 | 정배열(MA20 > MA60) / 역배열. **항상 값이 존재** |
| **F-TREND-03** | 교차 감지 | 최근 20거래일 이내 상향·하향 교차 탐지 |
| **F-TREND-04** | 위양성 필터 | 이격 필터 + 거래량 확인. §7.5 |
| **F-TREND-05** | 경과일 표시 | "N거래일 전 상향 교차" |
| **F-EXPL-01** | 템플릿 설명 | 결정론적 문장 생성. LLM 미사용 (PP-04) |
| **F-WATCH-01** | 관심종목 추가/삭제 | localStorage. 최대 20종목 |
| **F-WATCH-02** | 관심종목 리스트 | 위치·추세 동일 척도 나란히 표시 |
| **F-WATCH-03** | 정렬 | 위치 오름/내림, 추가순, 종목명순 |
| **F-STATE-01** | 데이터 부족 처리 | 최소 봉 수 미달 시 위치·추세 미표시 + 사유 안내 |
| **F-STATE-02** | 거래 상태 배지 | 거래정지·정리매매·투자경고·VI. `/stocks/{symbol}/warnings` |
| **F-STATE-03** | 휴장 처리 | 장 운영 캘린더 기반. "장 종료" / "휴장일" 표기 |
| **F-LEGAL-01** | 상시 Disclaimer | 모든 분석 화면 하단 고정. §13 |
| **F-LEGAL-02** | 계산 근거 시트 | `ⓘ` 탭 시 계산식·기간·한계 노출 |

### 4.2 Should Have — Phase 2

| ID | 기능 | 근거 |
|---|---|---|
| F-SPARK-01 | 미니 스파크라인 | 위치만으로는 "어느 방향에서 왔는지" 알 수 없다. 축·라벨 없는 얇은 선으로 최소한의 맥락 제공 |
| F-SPARK-02 | 스파크라인 위 현재 위치점 | 캐릭터 위치와 시계열의 연결고리 |
| F-AUTH-01 | Supabase Auth (이메일·소셜) | 기기 간 관심종목 동기화 |
| F-WATCH-04 | 관심종목 서버 동기화 | 로그인 시 |
| F-WARN-01 | 유의사항 상세 | 공매도·신용거래 잔고 등 |
| F-A11Y-01 | 스크린리더 대응 강화 | 캐릭터 위치의 텍스트 대체 |

### 4.3 Later — Phase 3 이후

| ID | 기능 | 보류 사유 |
|---|---|---|
| F-LT-01 | MA50 / MA200 장기 관점 | 봉 수 2배 필요, 연 0~2회 발생으로 화면이 대부분 빔 |
| F-LT-02 | 지수 대비 상대 위치 | 개념 난이도가 타깃과 맞지 않음 |
| F-LT-03 | 푸시 알림 (구간 변경·교차 발생) | **알림은 그 자체가 행동 촉구다.** 자문업 경계 재검토 필요 |
| F-LT-04 | 종목 뉴스 | 추가 API 필요 |
| F-LT-05 | 재무 지표 | 타깃 사용자의 이해 범위 밖 |
| F-LT-06 | 종목 로고 | 시각적 개선일 뿐 |

> **F-LT-03에 대한 주의.** "삼성전자가 무릎 구간에 진입했습니다"라는 푸시는 사용자가 요청하지 않은 시점에 특정 종목의 특정 가격대를 알리는 행위다. 이는 정보 제공보다 **투자 권유에 가깝게 해석될 여지**가 크다. 구현 전 반드시 별도 법률 검토를 거친다.

---

## 5. 화면 구성

### 5.1 공통 레이아웃 규칙

| 항목 | 규칙 |
|---|---|
| 기준 폭 | 375px (모바일 우선). 데스크톱은 최대 480px 중앙 정렬 |
| 하단 고정 | Disclaimer 1줄 (F-LEGAL-01) — 모든 분석 화면에서 스크롤과 무관하게 상시 노출 |
| 데이터 시각 | 화면 상단에 "YYYY-MM-DD HH:mm 기준" 상시 표시 |
| 색상 원칙 | **위치·구간에 따라 색을 바꾸지 않는다.** 등락률(빨강/파랑)만 예외 |

---

### 5.2 화면 1 — 종목 검색

| 항목 | 내용 |
|---|---|
| **목적** | 사용자가 알고 있는 종목명으로 대상을 특정 |
| **정보** | 검색 인풋 / 최근 검색어 / 검색 결과 리스트 (종목명·티커·시장 배지·현재가·등락률) |
| **CTA** | 결과 항목 탭 → 화면 2 |
| **UX** | · IME 조합 중 Enter 무시 (`isComposing`) — 한글 검색이 주 용도<br>· 입력 300ms 디바운스, 로컬 인덱스 조회이므로 네트워크 없음<br>· 초성 검색 지원 (F-SEARCH-03)<br>· KR/US 시장 필터 칩<br>· 모바일 자동 포커스 안 함 (키보드가 화면을 가림) |
| **상태값** | `IDLE` (최근 검색어) / `TYPING` / `RESULTS` / `EMPTY` / `INDEX_LOADING` / `INDEX_ERROR` |

> **검색 결과의 현재가 표시.** 결과가 20건이면 `/api/v1/prices`에 20개 심볼을 콤마로 묶어 **1회 호출**한다. 200건까지 다건 조회가 가능하므로 결과 수에 관계없이 호출 1회로 끝난다.

---

### 5.3 화면 2 — 위치 (종목 상세 기본 탭)

| 항목 | 내용 |
|---|---|
| **목적** | 현재가가 선택 기간의 가격 분포에서 어디에 위치하는지를 단일 시각으로 전달 |
| **정보** | ① 종목명·티커·시장 배지<br>② 현재가 + 전일 대비 등락률<br>③ **캐릭터 + 위치 마커**<br>④ 구간 라벨 + 퍼센타일 수치 ("무릎 · 22%")<br>⑤ 사실 진술 문장 (§7.6-A)<br>⑥ 기간 토글 (60/120/250)<br>⑦ 기간 최고가·최저가<br>⑧ 거래 상태 배지 (해당 시)<br>⑨ `ⓘ` 계산 근거 |
| **CTA** | · 관심종목 추가 (♡)<br>· 탭 전환 → 화면 3<br>· `ⓘ` → 계산 근거 시트 |
| **UX** | · 캐릭터는 정면 전신, **중립 표정 고정**<br>· 마커는 신체 우측에 수평선 + 라벨<br>· 기간 토글 전환 시 마커가 **애니메이션으로 이동** (400ms). 값이 튀는 게 아니라 **같은 종목의 다른 척도**임을 체감시킴<br>· `prefers-reduced-motion` 시 애니메이션 생략 |
| **상태값** | `LOADING` / `READY` / `INSUFFICIENT_DATA` / `HALTED` / `ERROR` |

> **왜 기간 토글 전환에 애니메이션을 넣는가.** 같은 종목이 60일 기준 머리, 250일 기준 무릎일 수 있다. 값이 순간적으로 교체되면 "앱이 틀렸다"고 느낀다. 마커가 미끄러져 이동하면 "기준을 바꾸니 위치가 달라졌다"는 인과가 전달된다. **이 앱에서 가장 중요한 마이크로 인터랙션이다.**

---

### 5.4 화면 3 — 흐름 (추세)

| 항목 | 내용 |
|---|---|
| **목적** | 단기 흐름과 장기 흐름의 관계, 그리고 최근 교차 여부를 전달 |
| **정보** | ① **배열 상태** — 정배열/역배열 (항상 값 존재)<br>② **교차 배지** — 있을 때만. "5거래일 전 상향 교차"<br>③ 두 선의 상대 위치 시각화 (§5.4-a)<br>④ 이격률 (%)<br>⑤ 사실 진술 문장 (§7.6-B)<br>⑥ `ⓘ` 계산 근거 |
| **CTA** | 탭 전환 → 화면 2 / 화면 4 |
| **UX** | · **MA 원값(예: 71,240원)은 기본 접힘.** 초보자에게 이 숫자는 의미 없음. "자세히" 탭 시 노출<br>· 교차가 없으면 배지 영역을 **숨긴다** (빈 카드 금지)<br>· 용어는 **"상향 교차 / 하향 교차"**를 주 표기로, `(골든크로스)`를 괄호 병기 |
| **상태값** | `LOADING` / `ALIGNED_UP` / `ALIGNED_DOWN` / `INSUFFICIENT_DATA` / `ERROR` |

#### 5.4-a. 두 선의 상대 위치 시각화

전체 캔들 차트를 그리지 않는다. **최근 60거래일의 MA20·MA60 두 선만** 축·눈금 없이 얇게 그린다.

```
        ╭─────────  MA20 (굵은 선)
   ────╯
  ───────────────  MA60 (얇은 선)
        ↑
     교차 지점 마커
```

- Y축 눈금·가격 라벨 **없음**. 두 선의 상하 관계와 교차 지점만이 전달 대상
- 교차 지점에 점 마커 + 경과일 라벨
- 캔들·거래량 **표시하지 않음**

> **"골든크로스"라는 단어를 주 표기로 쓰지 않는 이유.** '골든'은 그 자체로 긍정 가치를 담은 단어다. 정보형 앱이 중립을 표방하면서 지표 이름에 가치판단을 담는 것은 일관되지 않는다. 다만 사용자가 외부에서 접하는 용어이므로 괄호로 병기해 검색 가능성은 유지한다.

---

### 5.5 화면 4 — 쉬운 설명

| 항목 | 내용 |
|---|---|
| **목적** | 화면 2·3의 수치를 문장으로 재진술. 수치를 읽지 못하는 사용자를 위한 최종 안전망 |
| **정보** | 템플릿 문장 3~4줄 (§7.6) + 각 문장 옆 `ⓘ` |
| **CTA** | 관심종목 추가 / 다른 종목 검색 |
| **UX** | · 문장당 1줄, 최대 4줄<br>· **결론 문장을 만들지 않는다** (PP-05). 위치 문장과 추세 문장은 병렬 나열하며 접속사로 잇지 않음<br>· 폰트는 본문보다 1단계 크게 |
| **상태값** | `READY` / `PARTIAL` (일부 지표만 계산 가능) / `UNAVAILABLE` |

> **금지 예시.** "최근 범위에서 낮은 위치이며, 단기 이동평균선이 상향 돌파했습니다." — `~이며`로 두 사실을 잇는 순간 종합 판정이 된다. 두 문장으로 분리하고 사이에 시각적 구분선을 둔다.

---

### 5.6 화면 5 — 관심종목

| 항목 | 내용 |
|---|---|
| **목적** | 복수 종목의 위치·추세를 **동일 척도로 나란히** 비교 |
| **정보** | 종목별 행: 종목명 / 현재가 / 등락률 / **구간+퍼센타일** / **배열 상태 아이콘** / 교차 배지(있을 때) |
| **CTA** | 행 탭 → 화면 2 / 검색 아이콘 → 화면 1 / 편집 모드 |
| **UX** | · 기간 토글이 **리스트 전체에 일괄 적용** (종목별 개별 설정 없음 — 비교 척도가 깨짐)<br>· 정렬: 위치순 / 추가순 / 종목명순<br>· 위치는 미니 바(0~100)로도 병기해 시각적 비교 가능<br>· 20종목 초과 시 추가 차단 + 안내 |
| **상태값** | `EMPTY` (최초) / `LOADING` / `READY` / `PARTIAL_ERROR` (일부 종목 실패) |

> **성능 설계.** 관심종목 N개의 현재가는 `/api/v1/prices`에 N개 심볼을 콤마로 묶어 **1회 호출**한다. 과거 종가 배열은 서버 캐시에 있으므로, 퍼센타일·MA는 **캔들 API 호출 없이** 재계산된다. 즉 관심종목 화면 전체가 **토스 API 호출 1회**로 갱신된다. (§10.4)

---

### 5.7 상태 전이도

```
                    ┌──────────┐
        진입 ──────▶│  BOOTING │ 종목 마스터 인덱스 로드
                    └────┬─────┘
                         │
              ┌──────────┴──────────┐
              ▼                     ▼
       ┌────────────┐        ┌────────────┐
       │ WATCH_EMPTY│        │ WATCH_LIST │
       └─────┬──────┘        └─────┬──────┘
             └──────┬───────────────┘
                    ▼
             ┌────────────┐
             │   SEARCH   │
             └─────┬──────┘
                   ▼
             ┌────────────┐   봉 수 미달   ┌──────────────────┐
             │ DETAIL_POS │──────────────▶│ INSUFFICIENT_DATA│
             └─────┬──────┘                └──────────────────┘
                   │              거래정지  ┌──────────────────┐
                   ├────────────────────▶ │      HALTED      │
                   │                       └──────────────────┘
        ┌──────────┼──────────┐
        ▼          ▼          ▼
  DETAIL_TREND  DETAIL_EXPL  ERROR
```

---

## 6. 데이터 구조

### 6.1 Phase 1 — 서버 캐시 (DB 최소화)

Phase 1은 사용자 계정이 없고 관심종목이 로컬에 저장되므로, **영속 DB가 필요한 것은 시세 캐시뿐이다.** SQLite(단일 파일) 또는 Postgres 중 택일하되, 스키마는 동일하다.

```sql
-- 종목 마스터 (일 1회 배치 동기화)
CREATE TABLE stock (
  symbol          TEXT PRIMARY KEY,        -- '005930', 'AAPL'
  name_ko         TEXT,                    -- '삼성전자'
  name_en         TEXT,                    -- 'Samsung Electronics'
  initials        TEXT,                    -- 'ㅅㅅㅈㅈ' (초성 검색용, 사전 계산)
  market          TEXT NOT NULL,           -- 'KR' | 'US'
  exchange        TEXT,                    -- 'KRX' | 'NASDAQ' | 'NYSE' ...
  currency        TEXT NOT NULL,           -- 'KRW' | 'USD'
  listing_status  TEXT NOT NULL,           -- 'LISTED' | 'DELISTED' | 'SUSPENDED'
  shares_out      BIGINT,
  synced_at       TIMESTAMPTZ NOT NULL
);
CREATE INDEX idx_stock_search ON stock (market, listing_status);

-- 일봉 캐시 (수정주가 기준)
CREATE TABLE price_candle (
  symbol      TEXT NOT NULL,
  trade_date  DATE NOT NULL,
  open        NUMERIC(20,4) NOT NULL,
  high        NUMERIC(20,4) NOT NULL,
  low         NUMERIC(20,4) NOT NULL,
  close       NUMERIC(20,4) NOT NULL,
  volume      BIGINT NOT NULL,
  adjusted    BOOLEAN NOT NULL DEFAULT TRUE,
  fetched_at  TIMESTAMPTZ NOT NULL,
  PRIMARY KEY (symbol, trade_date)
);
CREATE INDEX idx_candle_symbol_date ON price_candle (symbol, trade_date DESC);

-- 계산 결과 스냅샷 (장 마감 후 배치, 선택적 최적화)
CREATE TABLE indicator_snapshot (
  symbol            TEXT NOT NULL,
  base_date         DATE NOT NULL,
  period_days       INT  NOT NULL,          -- 60 | 120 | 250
  percentile        NUMERIC(5,2),           -- 0.00 ~ 100.00
  body_zone         TEXT,                   -- 'FOOT'|'KNEE'|'WAIST'|'CHEST'|'SHOULDER'|'HEAD'
  period_high       NUMERIC(20,4),
  period_low        NUMERIC(20,4),
  ma_short          NUMERIC(20,4),          -- MA20
  ma_long           NUMERIC(20,4),          -- MA60
  alignment         TEXT,                   -- 'UP' | 'DOWN'
  gap_ratio         NUMERIC(8,5),           -- (ma_short-ma_long)/ma_long
  cross_type        TEXT,                   -- 'GOLDEN' | 'DEAD' | NULL
  cross_date        DATE,
  cross_days_ago    INT,
  cross_vol_ok      BOOLEAN,
  data_points       INT NOT NULL,           -- 계산에 사용된 실제 봉 수
  computed_at       TIMESTAMPTZ NOT NULL,
  PRIMARY KEY (symbol, base_date, period_days)
);

-- 토스 액세스 토큰 (단일 행)
CREATE TABLE toss_token (
  id           INT PRIMARY KEY DEFAULT 1,
  access_token TEXT NOT NULL,
  expires_at   TIMESTAMPTZ NOT NULL,
  CHECK (id = 1)
);
```

> **`indicator_snapshot`은 선택 사항이다.** 캔들 배열이 캐시에 있으면 퍼센타일·MA 계산은 수 밀리초면 끝난다. 스냅샷 테이블은 **관심종목 화면에서 20종목을 동시에 계산할 때의 응답 시간을 줄이기 위한 최적화**이며, Phase 1에서는 없어도 동작한다. 실측 후 필요하면 도입한다.

### 6.2 클라이언트 로컬 저장 (Phase 1)

```ts
// localStorage
interface LocalState {
  watchlist: {
    symbol: string;
    addedAt: string;      // ISO 8601
  }[];                     // 최대 20
  recentSearches: string[];  // 최대 10, 심볼
  preferences: {
    periodDays: 60 | 120 | 250;   // 기본 120
    watchlistSort: 'position' | 'added' | 'name';
    marketFilter: 'ALL' | 'KR' | 'US';
  };
  disclaimerAckAt: string | null;  // 최초 1회 고지 확인 시각
}
```

### 6.3 Phase 2 — 사용자 계정 도입 시

```sql
-- Supabase Auth의 auth.users를 참조
CREATE TABLE watchlist_item (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id     UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  symbol      TEXT NOT NULL REFERENCES stock(symbol),
  sort_order  INT  NOT NULL DEFAULT 0,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE (user_id, symbol)
);

CREATE TABLE user_preference (
  user_id     UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  period_days INT  NOT NULL DEFAULT 120,
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

ALTER TABLE watchlist_item ENABLE ROW LEVEL SECURITY;
CREATE POLICY watchlist_own ON watchlist_item
  USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);
```

### 6.4 API 응답 계약 (프론트 ↔ 우리 서버)

프론트는 토스 API를 절대 직접 호출하지 않는다. 우리 서버가 가공한 아래 형태만 받는다.

```ts
// GET /api/stock/{symbol}?period=120
interface StockAnalysisResponse {
  symbol: string;
  name: string;
  market: 'KR' | 'US';
  currency: 'KRW' | 'USD';

  price: {
    current: number;
    changeAmount: number;
    changeRate: number;        // -0.0123 = -1.23%
    asOf: string;              // ISO 8601
    isRealtime: boolean;       // 장중 여부
  };

  position: {
    available: boolean;
    reason?: 'INSUFFICIENT_DATA' | 'HALTED';
    periodDays: 60 | 120 | 250;
    dataPoints: number;        // 실제 사용된 봉 수
    percentile: number | null; // 0~100
    zone: BodyZone | null;
    periodHigh: number | null;
    periodLow: number | null;
    periodStartDate: string | null;
  };

  trend: {
    available: boolean;
    reason?: 'INSUFFICIENT_DATA';
    maShort: number | null;    // MA20
    maLong: number | null;     // MA60
    alignment: 'UP' | 'DOWN' | null;
    gapRatio: number | null;
    cross: {
      type: 'GOLDEN' | 'DEAD';
      date: string;
      daysAgo: number;
      volumeConfirmed: boolean;
    } | null;
    maSeries: { date: string; short: number; long: number }[];  // 최근 60일
  };

  warnings: {
    code: string;              // 'TRADING_HALT' | 'ADMIN_ISSUE' | 'VI' | ...
    label: string;             // 사용자 표시 문구
  }[];

  explanations: string[];      // 템플릿 생성 문장 배열
}

type BodyZone = 'FOOT' | 'KNEE' | 'WAIST' | 'CHEST' | 'SHOULDER' | 'HEAD';
```

```ts
// GET /api/watchlist?symbols=005930,AAPL,...&period=120
interface WatchlistResponse {
  asOf: string;
  items: {
    symbol: string;
    name: string;
    market: 'KR' | 'US';
    price: { current: number; changeRate: number };
    position: { percentile: number | null; zone: BodyZone | null };
    trend: { alignment: 'UP' | 'DOWN' | null; crossType: 'GOLDEN' | 'DEAD' | null; crossDaysAgo: number | null };
    error?: string;   // 개별 종목 실패 시
  }[];
}
```

---

## 7. 지표 계산 로직

### 7.1 공통 전제

| 항목 | 규칙 |
|---|---|
| 가격 기준 | **수정주가 종가** (`adjusted=true` 명시 고정) |
| 시장 구분 | KR·US 동일 로직 적용 |
| **거래일 환산** | 봉의 `timestamp`를 **시장 현지 타임존으로 환산한 날짜**가 거래일이다. KR=`Asia/Seoul`, US=`America/New_York`. 문자열을 자르거나 UTC로 환산하지 않는다 (아래 실측 ③) |
| 당일 봉 | 위치 계산의 **분포에는 당일을 포함하지 않는다.** 직전 N거래일의 종가 분포와 **현재가**를 비교 |
| **당일 봉의 성격** | 배열의 마지막 원소는 "어제 종가"가 아니라 **아직 마감되지 않은 진행 중인 봉**일 수 있다 (아래 실측 ④). §7.4가 장중에 현재가를 대입하는 대상이 이 봉이다 |
| 결측일 | 휴장·거래정지로 봉이 없는 날은 배열에서 제외한 뒤 인덱싱 (달력일이 아닌 **거래일** 기준) |
| 부동소수 비교 | 상대오차 `1e-9` 허용 |

#### Phase 0-4 실측 반영 (2026-08-27)

> 실측 원본: `docs/probe/`, 스냅샷 `docs/toss-openapi.snapshot.json`
> 구현·회귀 테스트: `src/lib/toss/trading-day.ts`, `tests/trading-day.test.ts`

**① `/stocks/all`은 한글 종목명을 제공한다 — F-SEARCH-01/03 성립.**
필드는 `symbol, name, securityType, isCommonShare, isinCode`. 예상과 달랐던 것은 **미국 종목에도 한글 표기가 붙는다**는 점이다(`"ATA 크리에티비티 글로벌(ADR)"`, `"아포지 애퀴지션"`). 미국 종목 10,969개 중 5,831개(53%)에 한글명이 있다. 초성 인덱스를 미국 종목에도 붙이며, `"ㅇㅍ"`로 AAPL이 검색되는 것은 버그가 아니라 F-SEARCH-03의 이득이다.
다만 `/stocks/all`에는 **영문명이 없다.** F-SEARCH-02(영문 검색)를 위해 §8.1의 마스터 동기화를 2단계로 바꾼다.

**② `before` 페이지네이션으로 250봉 소급이 가능하다 — §8.2 그대로 성립.**
005930 기준 1회차 200봉 + 2회차 50봉 = 250봉. `nextBefore`는 응답 최상위에 오고, 오프셋의 `+`는 `%2B` 인코딩이 필요하다는 §8.2의 주의사항도 실측과 일치했다.

**③ 미국 종목 봉의 `timestamp`는 "뉴욕 자정"을 KST 오프셋으로 표기한 값이다.**
오프셋 문자열은 **언제나 `+09:00`**이지만 벽시계 시각이 계절에 따라 갈린다.

| 시각 | 봉 수 | 구간 | 의미 |
|---|---|---|---|
| `13:00+09:00` | 120봉 | 2026-03-09 ~ | 자정 EDT (서머타임) |
| `14:00+09:00` | 80봉 | ~ 2026-03-06 | 자정 EST (표준시) |

경계가 2026-03-08(미국 서머타임 시작일)과 정확히 일치한다. **오프셋 문자열은 시장을 알려주지 않는다.** 따라서 거래일은 시장 현지 타임존으로 환산해서 얻는다. 지금은 문자열 앞 10자를 잘라도 같은 답이 나오지만, 그건 자정 ET에 13~14시간을 더해도 당일을 넘지 않는다는 우연에 기댄 것이다. 토스가 오프셋 표기를 바꾸면 조용히 하루씩 어긋난다.

**④ 캔들 배열의 최신 봉은 진행 중인 미완성 봉이다. (예상 밖 발견)**
프로브를 3분 간격으로 두 번 돌렸더니 같은 날짜의 최신 봉 값이 바뀌었다.

| 종목 | 최신 봉 거래량 | 직전 20봉 중앙값 | 비율 | 상황 |
|---|---|---|---|---|
| AAPL | 93,076 | 44,812,503 | **0.2%** | 정규장 개장 전(22:30 KST), 프리마켓 체결만 누적 |
| 005930 | 26,273,347 | 46,036,748 | **57.1%** | 장중 15:19 (09:00~15:30) |

AAPL은 3분 사이 종가 310.61 → 310.69, 거래량 93,043 → 93,076으로 실시간 갱신됐다. §7.1의 "분포에서 당일 제외"와 §7.4의 "장중이면 최신 봉 종가에 현재가 대입"은 둘 다 **"마지막 원소 = 당일 봉"을 전제**하므로, 그 전제가 실제로 성립하는지 판정할 수단이 필요하다 → `isFormingBar()`.

### 7.2 위치 계산 — 퍼센타일 (Midrank)

```
입력:
  C          = 현재가 (실시간 또는 최종 체결가)
  P[1..N]    = 직전 N거래일의 수정주가 종가 배열 (N = 60 | 120 | 250)

계산:
  below = |{ i : P[i] < C }|          // 현재가보다 낮은 종가의 개수
  equal = |{ i : P[i] == C }|         // 현재가와 같은 종가의 개수

  percentile = (below + 0.5 × equal) / N × 100
```

**Midrank(동점에 0.5 가중)를 쓰는 이유.** 저가주나 거래가 뜸한 종목은 동일 종가가 반복된다. `below/N`만 쓰면 이런 종목이 체계적으로 낮게 표시되고, `(below+equal)/N`을 쓰면 체계적으로 높게 표시된다. Midrank는 양쪽으로 치우치지 않는다.

**계산 예시** — 삼성전자, N=120, 현재가 74,500원

```
직전 120거래일 종가 중
  74,500원 미만인 날 : 38일
  74,500원과 같은 날 :  2일

percentile = (38 + 0.5 × 2) / 120 × 100
           = 39 / 120 × 100
           = 32.5%
→ 구간: WAIST (허리)   ← §7.3 경계값 표 기준 (30 ≤ p < 55)
→ 문구: "최근 120거래일 중 약 33%의 날보다 높습니다."
```

**Min-Max 대비 이점 (설계 기록):**

| 상황 | Min-Max | 퍼센타일 |
|---|---|---|
| 기간 중 하루만 30% 급등 | 이후 119일간 위치가 20%대에 고착 | 그 하루는 표본 1개일 뿐, 영향 미미 |
| 모든 종가가 동일 (극단) | `(C-L)/(H-L)` → **0/0 division by zero** | 50.0% 로 자연 수렴 |
| 6구간 분포 | 중앙(허리)에 집중 | 균등에 가까움 |
| 화면 문구 | "범위의 32% 지점" (해석) | "33%의 날보다 높음" (**사실**) |

**보조 표시.** 기간 최고가·최저가는 별도 수치로 병기한다. 캐릭터 위치(퍼센타일)와 고저 수치(Min-Max)는 **서로 다른 것을 재는 값**이며, 이를 `ⓘ` 시트에서 명시한다.

### 7.3 신체 구간 매핑

| 구간 | 코드 | 퍼센타일 범위 | 인체 해부학 기준 높이 |
|---|---|---|---|
| 발 | `FOOT` | 0 ≤ p < 10 | ~0–5% |
| 무릎 | `KNEE` | 10 ≤ p < 30 | ~28% |
| 허리 | `WAIST` | 30 ≤ p < 55 | ~58% |
| 가슴 | `CHEST` | 55 ≤ p < 72 | ~70% |
| 어깨 | `SHOULDER` | 72 ≤ p < 85 | ~82% |
| 머리 | `HEAD` | 85 ≤ p ≤ 100 | ~90–100% |

**경계 히스테리시스 (F-POS-05)**

```
이전 구간이 Z이고 현재 퍼센타일이 p일 때:

  if (p가 Z의 범위 안) → Z 유지
  else if (p가 Z의 경계에서 2.0%p 이내로만 벗어남) → Z 유지
  else → p에 해당하는 새 구간으로 전환

이전 구간 상태는 클라이언트 세션 메모리에 보관 (영속화하지 않음)
```

29.5% ↔ 30.5%를 오갈 때마다 "무릎 ↔ 허리"가 번갈아 표시되면 앱 신뢰도가 무너진다. **표시되는 퍼센타일 수치는 항상 실제 값**이고, 히스테리시스는 **구간 라벨에만** 적용된다.

### 7.4 이동평균 (SMA)

```
MA_n[t] = ( Σ_{i=0}^{n-1} Close[t-i] ) / n

MA20 : n = 20
MA60 : n = 60

- 단순이동평균(SMA). EMA 아님
- 수정주가 종가 기준
- 최신 봉(t)은 장중이면 현재가를 종가로 대입 (isRealtime = true 표시)
```

**KR·US 통일 근거 (설계 기록)**

| 조합 | 연간 교차 빈도 | 필요 봉 수 | 판정 |
|---|---|---|---|
| MA5 / MA20 | 15~25회 | 21 | 노이즈 과다 |
| **MA20 / MA60** | **3~6회** | **61** | ✅ 채택 |
| MA50 / MA200 | 0~2회 | 201 | 화면이 대부분 빔, 봉 수 부담 |

미국 종목에 MA50/200(시장 관습)을 적용하지 않는 이유는 세 가지다. ① 봉 수가 201개로 늘어 호출이 증가한다. ② 관심종목 화면에서 KR과 US의 기준이 달라지면 **동일 척도 비교라는 화면 5의 전제가 무너진다.** ③ 대형주 기준 연 0~2회 발생이라 대부분의 시점에 표시할 내용이 없다.

### 7.5 교차 판정 + 위양성 필터

```
1단계 — 원시 교차 감지 (최근 20거래일 순회, 최신 → 과거)

  for t in [최신, 최신-1, ..., 최신-19]:
      if MA20[t-1] <= MA60[t-1] and MA20[t] > MA60[t]:
          candidate = { type: 'GOLDEN', date: t }
          break
      if MA20[t-1] >= MA60[t-1] and MA20[t] < MA60[t]:
          candidate = { type: 'DEAD',   date: t }
          break

  → 가장 최근 교차 1건만 채택. 20일 내 없으면 cross = null

2단계 — 이격 필터 (횡보 whipsaw 제거)

  gapRatio = |MA20[t] - MA60[t]| / MA60[t]
  if gapRatio < 0.005:            // 0.5% 미만
      cross = null                // 교차로 인정하지 않음

3단계 — 거래량 확인 (필터 아님, 표시용)

  avgVol20 = 교차일 직전 20거래일 거래량 평균
  volumeConfirmed = (Volume[t] >= avgVol20)

  → false여도 교차는 표시하되 "거래량 미확인" 배지 부착

4단계 — 유효기간

  daysAgo = 교차일로부터 경과 거래일 수
  if daysAgo > 20:
      cross = null                // "3개월 전 골든크로스"는 현재 상태가 아님
```

**확인 지연(교차 후 N일 유지 시 확정)을 넣지 않은 이유.** 위양성은 줄지만 ① 신선도가 떨어지고 ② "어제 교차했으나 아직 확정 전"이라는 중간 상태를 초보자에게 설명할 방법이 없다. 이격 필터가 whipsaw의 대부분을 제거하므로 충분하다고 판단한다.

### 7.6 설명 문구 템플릿 (결정론적, PP-04)

**A. 위치 문장**

```
[필수] "최근 {periodDays}거래일 중 약 {round(percentile)}%의 날보다 높습니다."

[조건부] 데이터 부족:
        "가격 데이터가 {dataPoints}일치만 있어 위치를 계산하지 않았습니다."

[보조]  "이 기간의 최고가는 {periodHigh}원, 최저가는 {periodLow}원입니다."
```

**B. 추세 문장**

```
[필수] alignment == 'UP'
        → "20일 평균 가격이 60일 평균 가격보다 높습니다."
       alignment == 'DOWN'
        → "20일 평균 가격이 60일 평균 가격보다 낮습니다."

[조건부] cross != null && type == 'GOLDEN'
        → "{daysAgo}거래일 전, 20일 평균선이 60일 평균선을 위로 지나갔습니다."
       cross != null && type == 'DEAD'
        → "{daysAgo}거래일 전, 20일 평균선이 60일 평균선을 아래로 지나갔습니다."

[조건부] volumeConfirmed == false
        → "다만 그날의 거래량은 최근 평균보다 적었습니다."
```

**C. 문장 조립 규칙**

| 규칙 | 내용 |
|---|---|
| R-01 | 위치 문장과 추세 문장 사이에 **접속사를 쓰지 않는다.** 시각적 구분선으로 분리 |
| R-02 | **종합 판정 문장을 생성하지 않는다** (PP-05) |
| R-03 | 총 4문장 이내 |
| R-04 | 모든 변수는 서버에서 포맷팅 후 전달 (통화 기호·자릿수 포함) |
| R-05 | §13 금지 표현 목록에 있는 단어는 템플릿에 등장할 수 없다. **CI에서 정적 검사로 강제** |

> **R-05는 린트 규칙으로 구현한다.** 템플릿 상수 파일(`lib/templates.ts`)을 대상으로 금지어 목록을 검사하고, 위반 시 빌드를 실패시킨다. 사람의 주의력에 의존하지 않는다.

### 7.7 최소 데이터 요구량

| 계산 | 최소 봉 수 | 미달 시 동작 |
|---|---|---|
| 퍼센타일 (period=60) | 40 | 위치 미표시 + 사유 안내 |
| 퍼센타일 (period=120) | 80 | period를 60으로 자동 강등 + 안내 |
| 퍼센타일 (period=250) | 160 | period를 120으로 자동 강등 + 안내 |
| MA20 | 21 | 추세 전체 미표시 |
| MA60 | 61 | 추세 전체 미표시 |
| 교차 감지 | 61 + 최소 1일 | 배열 상태만 표시, 교차 미표시 |

**자동 강등 시 반드시 안내한다.** "상장 후 데이터가 짧아 60거래일 기준으로 표시합니다." 조용히 기준을 바꾸면 PP-03 위반이다.

---

## 8. API Mapping

### 8.1 기능 ↔ 토스증권 엔드포인트 매핑

| 기능 | 필요 데이터 | 토스 API 엔드포인트 | 가공 필요 | 비고 |
|---|---|---|---|---|
| F-SEARCH-01/02 | 종목 마스터 (KR·US 전체) | `GET /api/v1/stocks/all?market=…`<br>→ `GET /api/v1/stocks?symbols=…` | ✅ **높음** | 이름 검색 API 없음. 전체 마스터를 받아 **자체 인덱스 구축**. ⚠️ 2단계 구성 — §8.1-a |
| F-SEARCH-03 | 초성 문자열 | — | ✅ | `name_ko`에서 사전 계산해 `stock.initials`에 저장 |
| F-SEARCH-01 (결과 시세) | 검색 결과 현재가 | `GET /api/v1/prices?symbols=A,B,C` | ❌ | **최대 200종목 다건 1회 호출** |
| F-POS-01/06 | 직전 250거래일 OHLCV | `GET /api/v1/candles?interval=1d&count=200&adjusted=true` | ✅ **높음** | **1회 200봉 상한** → 250봉은 `before`/`nextBefore`로 **2회 호출**. §8.2 |
| F-POS-06 (현재가) | 실시간 현재가 | `GET /api/v1/prices?symbols=...` | ❌ | 퍼센타일의 비교 대상 |
| F-TREND-01~05 | 동일 일봉 배열 | `GET /api/v1/candles` (위와 **동일 데이터 재사용**) | ✅ **높음** | **추가 호출 없음.** MA·교차 전량 자체 계산 |
| F-STATE-02 | 매수 유의사항 | `GET /api/v1/stocks/{symbol}/warnings` | ✅ 낮음 | 정리매매·단기과열·투자경고/위험·VI·신주인수권 |
| F-POS-06 (종목명) | 종목 기본정보 | `GET /api/v1/stocks?symbols=...` | ❌ | 마스터 캐시로 대체 가능. 상장상태 확인용으로만 사용 |
| F-STATE-03 | 장 운영 시간 | `GET /api/v1/market-calendar/KR`<br>`GET /api/v1/market-calendar/US` | ✅ 낮음 | 휴장 판정, 캐시 갱신 스케줄 결정 |
| F-WATCH-02 | 관심종목 일괄 시세 | `GET /api/v1/prices?symbols=...` | ❌ | **N종목 1회 호출** |
| (US 종목 원화 환산) | 환율 | `GET /api/v1/exchange-rate` | ❌ | Phase 2. MVP는 원통화 표시 |

### 8.1-a 마스터 동기화 2단계 (Phase 0-4 실측 정정)

초판은 `/stocks/all` 단일 호출로 마스터가 끝난다고 적었다. 실측 결과 둘 다 틀렸다.

**정정 1 — `market`은 필수 파라미터다.** 생략하면 `400 invalid-request (field=market)`이 떨어진다. 전체 마스터는 세그먼트 7개만큼의 호출로 구성된다.

**정정 2 — `/stocks/all`은 영문명·통화·상장상태·발행주식수를 주지 않는다.** `symbol, name, securityType, isCommonShare, isinCode`가 전부다. §6.1 스키마의 `name_en`·`currency`·`shares_out`을 채우려면 `/stocks?symbols=`가 필요하다. 이쪽은 `englishName, market, currency, status, listDate, delistDate, sharesOutstanding`까지 준다.

```
[1] GET /api/v1/stocks/all?market={KOSPI|KOSDAQ|NYSE|NASDAQ|AMEX|KR_ETC|US_ETC}
    STOCK_ALL · 1 TPS · 7회
    → 심볼 목록. 실측 15,272종목 (KR 4,303 / US 10,969)

[2] GET /api/v1/stocks?symbols={200개씩}
    STOCK · 5 TPS · 77회 (= ceil(15272/200))
    → englishName·currency·status·sharesOutstanding 채움

[3] name_ko 에서 초성 사전 계산 → stock.initials
[4] 이번 동기화에서 갱신되지 않은 행 = 목록에서 빠진 종목 → DELISTED 표시
    (행을 지우지 않는다. 관심종목에 남아 있을 때 "상장폐지"라고 알려주는 편이
     조용히 사라지는 것보다 낫다 — PP-03)
```

실측 소요 약 20초. 일 1회 배치이므로 여유롭다. 구현: `scripts/sync-master.ts`.

> **[4]는 실패가 하나라도 있으면 건너뛴다.** 배치 일부가 실패한 상태에서 "갱신 안 됨 = 상장폐지"로 판정하면 멀쩡한 종목이 대량으로 상장폐지 처리된다.

### 8.2 250봉 확보 시퀀스

```
1) GET /api/v1/candles?symbol=005930&interval=1d&count=200&adjusted=true
   → candles[0..199] (최신순), nextBefore = "2025-xx-xxT..."

2) GET /api/v1/candles?symbol=005930&interval=1d&count=50&adjusted=true
        &before={nextBefore를 URL 인코딩}
   → candles[200..249]

3) 두 배열을 병합하고 trade_date 오름차순 정렬 후 price_candle 테이블에 UPSERT

주의:
- before 파라미터의 타임존 오프셋 '+' 는 반드시 %2B 로 URL 인코딩
- 두 호출은 MARKET_DATA_CHART(20 TPS) 그룹. 큐를 통해 순차 실행
- 신규 상장 등으로 200봉 미만이면 2회차 호출을 생략
```

**핵심:** 250봉을 한 번 받아두면 **60·120·250 세 기간의 퍼센타일과 MA20/60·교차가 전부 이 하나의 배열에서 계산된다.** 기간 토글 전환 시 토스 API 호출은 **0회**다.

### 8.3 명시적으로 사용하지 않는 API

| 엔드포인트 | 미사용 사유 |
|---|---|
| `/api/v1/accounts`, `/api/v1/holdings` | 사용자 계좌를 다루지 않는다. `X-Tossinvest-Account` 헤더가 필요 없음 |
| `/api/v1/orders`, `/api/v1/conditional-orders` | **주문 기능을 제공하지 않는다.** 이 앱은 정보형이며 매매 경로를 갖지 않는다 |
| `/api/v1/orderbook`, `/api/v1/trades` | 호가·체결은 타깃 사용자의 이해 범위 밖 |
| `/api/v1/price-limits` | 상하한가는 KR 전용이며 이 앱의 지표와 무관 |
| `wss://openapi-ws.tossinvest.com` | **계정당 동시 연결 2개 제한** → 다중 사용자 실시간 푸시가 구조적으로 불가능. §10.5 |
| `/api/v1/rankings`, `/api/v1/market-indicators/*` | Phase 3 이후 |

> **주문 API를 쓰지 않는 것은 기술적 제약이 아니라 제품 결정이다.** 앱 안에 매수 버튼이 존재하는 순간, 위치·추세 표시는 정보 제공이 아니라 **매매 유도 화면**이 된다. §13의 법적 포지션 전체가 이 결정 위에 서 있다.

### 8.4 Rate Limit 예산

| 그룹 | 한도 | 우리 사용 패턴 | 여유 |
|---|---|---|---|
| `AUTH` | 5 TPS | 1시간에 1회 (토큰 캐싱) | 충분 |
| `STOCK_ALL` | **1 TPS** | **일 1회 배치** · 세그먼트당 1회 = **7회** (§8.1-a) | 빠듯 — 절대 사용자 요청 경로에 두지 않는다 |
| `MARKET_DATA_CHART` | 20 TPS | 캐시 미스 시 종목당 2회 | 큐 필요 |
| `MARKET_DATA` | 15 TPS | 현재가 다건 (사용자당 1회) | 충분 |
| `STOCK` | 5 TPS | warnings 조회 · **마스터 동기화 77회**(일 1회, §8.1-a) | 캐시 30분. 배치는 장 시작 전에 돌린다 |

**동시성 제어.** 프록시 레이어에 **그룹별 토큰 버킷 큐**를 둔다. 캐시 미스가 몰릴 때 `MARKET_DATA_CHART` 20 TPS를 넘지 않도록 요청을 직렬화하고, 대기 중인 클라이언트에는 스켈레톤을 유지한다. 429 수신 시 `Retry-After` 헤더 값만큼 대기 후 지수 백오프(1s → 2s → 4s) + jitter로 재시도한다.

---

## 9. 추가 API 필요성

### 9.1 토스증권 API만으로 완결되는 기능

**MVP(Phase 1) 전 범위.** 종목 검색, 위치 계산, 추세 계산, 설명 문구, 관심종목, 유의사항 배지, 휴장 처리 — **추가 API가 하나도 필요하지 않다.**

### 9.2 추가하면 좋아지는 것 (선택 · Phase 2)

| 항목 | 후보 | 얻는 것 | 비용 |
|---|---|---|---|
| 사용자 인증 | **Supabase Auth** | 기기 간 관심종목 동기화 | 무료 티어로 충분. 기존 스택 경험 있음 |
| 종목 로고 | 자체 에셋 또는 Clearbit | 검색·관심종목 리스트 시각적 식별성 | KR 종목은 로고 소스가 빈약. 티커 이니셜 폴백 필요 |
| 종목 뉴스 | 네이버 검색 API (뉴스) | "왜 움직였는지" 맥락 | 🔴 **주의:** 뉴스를 위치·추세 옆에 배치하면 인과 해석을 유도한다. PP-05와 충돌 가능. 별도 탭으로 완전 분리해야 함 |

### 9.3 추가 API 없이는 불가능한 것

| 항목 | 필요한 것 | Phase |
|---|---|---|
| 기기 간 관심종목 동기화 | 인증 + DB (Supabase) | 2 |
| 푸시 알림 | 웹푸시 / FCM | 3 (**법률 검토 선행**) |
| 기업 재무 지표 | DART Open API | 3 이후 |

### 9.4 결론

**MVP는 토스증권 Open API 단독으로 간다.** API를 늘리는 것이 목적이 아니라는 원칙에 부합하며, 실제로 필요하지도 않다. Phase 2에서 Supabase Auth 하나만 추가한다.

---

## 10. 기술 아키텍처

### 10.1 제약이 아키텍처를 결정한다

| 제약 | 출처 | 아키텍처 귀결 |
|---|---|---|
| **허용 IP 화이트리스트** | 토스 공식 문서 | 🔴 **고정 아웃바운드 IP를 가진 실행 환경 필수.** Vercel 서버리스 불가 |
| `client_secret` 보안 | 필수 요구사항 | 서버 전용 환경변수. 프론트 번들에 절대 미포함 |
| 캔들 200봉 상한 | 토스 공식 문서 | 종목당 2회 호출 + 캐시 |
| `STOCK_ALL` 1 TPS | 토스 공식 문서 | 마스터 동기화는 배치 전용 |
| WS 계정당 2연결 | 토스 공식 문서 | 실시간 푸시 포기, 폴링 |

### 10.2 채택 구성

```
┌─────────────────────────────────────────────────────────┐
│  브라우저 (PWA)                                          │
│   Next.js App Router / React / Tailwind                 │
│   · 검색 인덱스 (클라이언트 메모리, 앱 진입 시 1회 로드)      │
│   · 관심종목 (localStorage)                              │
│   · 토스 API 직접 호출 없음. 우리 도메인만 호출              │
└──────────────────────┬──────────────────────────────────┘
                       │  GET /api/stock/{symbol}?period=120
                       │  GET /api/watchlist?symbols=...
                       │  GET /search-index.json
                       ▼
┌─────────────────────────────────────────────────────────┐
│  Next.js 서버 (Route Handlers)  ★ 고정 IP 환경에서 실행     │
│   · TOSS_CLIENT_ID / SECRET 보관 (환경변수)                │
│   · 액세스 토큰 캐싱 (1시간)                                │
│   · 그룹별 토큰 버킷 큐 (Rate Limit 준수)                   │
│   · 지표 계산 (퍼센타일 · MA · 교차)                        │
│   · 설명 문구 템플릿 조립                                   │
│   · 응답 정규화 (§6.4 계약)                                │
└───────┬─────────────────────────────┬───────────────────┘
        │                             │
        ▼                             ▼
┌────────────────┐          ┌──────────────────────────┐
│ Postgres        │          │ openapi.tossinvest.com   │
│ (또는 SQLite)    │          │  · /oauth2/token          │
│ · stock          │          │  · /api/v1/stocks/all     │
│ · price_candle   │          │  · /api/v1/candles        │
│ · toss_token     │          │  · /api/v1/prices         │
└────────────────┘          │  · /api/v1/stocks/*/warnings│
                             └──────────────────────────┘
        ▲
        │  Cron
┌───────┴────────────────────────────────────────────────┐
│  배치 작업                                               │
│   · 종목 마스터 동기화 (일 1회, 06:00 KST)                 │
│   · 인기/관심종목 캔들 프리워밍 (KR 장마감 16:00, US 06:00) │
│   · 캔들 캐시 정리 (250봉 초과분 삭제)                      │
└────────────────────────────────────────────────────────┘
```

### 10.3 배포처 선택

| 안 | 구성 | 장점 | 단점 | 판정 |
|---|---|---|---|---|
| **A. 분리형** | Vercel(프론트) + 고정 IP VPS(프록시) | 익숙한 Vercel DX 유지 | 배포 대상 2개, CORS·인증 경계 추가, 지연 1홉 증가 | 차선 |
| **B. 통합형** | **Fly.io 또는 Railway에 Next.js 전체 + dedicated IP** | 단일 배포처, 코드 경계 단순, 지연 최소 | Vercel 고유 기능(ISR 최적화 등) 포기 | ✅ **채택** |
| C. Vercel Secure Compute | Vercel Enterprise | 스택 무변경 | Enterprise 요금제 필요 | 개인 프로젝트에 부적합 |

**B안 채택 이유.** 이 앱의 서버는 얇다 — 토스 API를 호출하고, 계산하고, JSON을 내려주는 것이 전부다. 이걸 위해 배포 대상을 둘로 쪼갤 이유가 없다. Fly.io의 dedicated IPv4는 유료지만 월 단위 비용이 미미하고, Postgres도 같은 플랫폼에서 붙는다.

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js 15 (App Router) | Route Handler로 BFF까지 한 코드베이스 |
| 언어 | TypeScript (strict) | 지표 계산 로직의 타입 안전성이 중요 |
| 스타일 | Tailwind CSS | |
| 상태 | TanStack Query | 캐싱·재시도·백그라운드 갱신 내장 |
| DB | Postgres (Fly Postgres) | 캔들 캐시. SQLite도 가능하나 확장성 고려 |
| 배포 | Fly.io + dedicated IPv4 | **고정 IP 필수 요건 충족** |
| 배치 | Fly Machines cron | |
| 차트 | **직접 SVG 렌더** | MA 2선 60포인트 + 스파크라인. 차트 라이브러리는 과함 |
| 모니터링 | Sentry (또는 Axiom) | 토스 API 실패율 추적 필수 |

> **차트 라이브러리를 쓰지 않는 이유.** 이 앱이 그리는 것은 축도 눈금도 없는 얇은 선 2개와 스파크라인뿐이다. Recharts나 Chart.js를 넣으면 번들이 수십 KB 늘어나고, 정작 필요한 "축 없는 미니멀 선"을 위해 기본 스타일을 전부 꺼야 한다. `<path>` 직접 생성이 더 짧고 정확하다.

### 10.4 캐시 전략

| 데이터 | TTL / 갱신 | 저장소 | 비고 |
|---|---|---|---|
| 액세스 토큰 | `expires_in` − 60초 | DB `toss_token` | 만료 60초 전 선제 갱신 |
| 종목 마스터 | 일 1회 배치 | DB `stock` | `STOCK_ALL` 1 TPS 제약 |
| 검색 인덱스 (클라) | 24시간 | 브라우저 (persist) | 진입 시 `/search-index.json` 1회. **실측 346KB (gzip)** — D-04 |
| **일봉 250개** | **다음 장 마감까지** | DB `price_candle` | 🔑 **핵심 캐시.** 하루 1회만 갱신 |
| 현재가 | **30초** | 메모리 (LRU) | 장중. 장 마감 후에는 다음 개장까지 |
| 유의사항 | 30분 | 메모리 | |
| 장 운영 캘린더 | 24시간 | 메모리 | |

**캐시 워밍 정책 (lazy + 선별 배치)**

```
[Lazy]  사용자가 조회한 종목 → 캐시 미스면 즉시 API 호출 → 다음 장마감까지 보관
[배치]  장 마감 후, 아래 종목의 캔들을 미리 갱신
          · 최근 7일간 조회된 적 있는 종목
          · 관심종목에 등록된 적 있는 종목 (Phase 2, 로그인 사용자)
        → 20 TPS 큐로 순차 처리

전 종목 프리워밍은 하지 않는다 (KRX+US 수천 종목 × 2회 = 낭비)
```

**이 캐시 설계의 효과:**

| 화면 | 캐시 히트 시 토스 API 호출 |
|---|---|
| 검색 결과 20건 | **1회** (prices 다건) |
| 종목 상세 최초 진입 | **1회** (prices) — 캔들은 캐시 |
| 기간 토글 전환 | **0회** |
| 화면 2↔3↔4 탭 전환 | **0회** |
| 관심종목 20종목 | **1회** (prices 다건) |

### 10.5 실시간성 정책

토스증권 WebSocket은 **계정당 동시 연결 2개**로 제한된다. 우리는 단일 credential로 전 사용자를 서빙하므로, **사용자별 실시간 스트림은 구조적으로 불가능하다.**

| 시점 | 갱신 방식 | 표시 |
|---|---|---|
| 장중 | 화면 포커스 시 30초 폴링 | "실시간" 배지 + 갱신 시각 |
| 장 마감 후 | 폴링 중단 | "N월 N일 종가 기준" |
| 휴장일 | 폴링 중단 | "휴장일 — 직전 거래일 기준" |

- `document.visibilitychange`로 백그라운드 탭 폴링 중단
- **실시간이 아님을 숨기지 않는다** (PP-03). "실시간"이라는 표현은 30초 폴링이 동작 중일 때만 사용하고, 그 외에는 기준 시각을 명시한다

---

## 11. API 보안 구조

### 11.1 자격증명 취급 원칙

| 항목 | 규칙 |
|---|---|
| `TOSS_CLIENT_ID` / `TOSS_CLIENT_SECRET` | **서버 환경변수에만 존재.** `.env.local`은 `.gitignore`, `.env.example`만 커밋 |
| 접근 코드 경로 | `lib/toss/client.ts` **단 한 파일**에서만 읽는다 |
| `import "server-only"` | `lib/toss/*` 최상단에 선언 → 클라이언트 import 시 **빌드 실패** |
| `NEXT_PUBLIC_` 접두사 | 토스 관련 변수에 **절대 사용 금지** |
| 액세스 토큰 | DB 저장. 응답 본문·로그·에러 메시지에 절대 노출 안 함 |
| 로깅 | 토큰·시크릿을 `console.log` 금지. 유효성은 `valid: true/false` 불리언만 |
| 에러 전파 | 토스 API 에러 원문을 클라이언트에 그대로 전달하지 않음. §11.3 매핑 테이블 경유 |

### 11.2 빌드 타임 강제

```
1) lib/toss/*.ts 최상단   → import "server-only";
2) ESLint 커스텀 룰       → NEXT_PUBLIC_TOSS_* 패턴 사용 시 error
3) 빌드 후 검증 스크립트   → .next/static/** 에서 client_secret 값·
                            'TOSS_CLIENT' 문자열 검색, 발견 시 exit 1
4) 템플릿 금지어 린트     → §7.6 R-05
```

`3)`은 실수를 사후에 잡는 최후 방어선이다. CI 파이프라인에 포함한다.

### 11.3 클라이언트 노출 에러 매핑

| 토스 응답 | 내부 코드 | 사용자 표시 |
|---|---|---|
| 401 `invalid-token` / `expired-token` | `AUTH_REFRESH` | (자동 재발급 후 1회 재시도. 사용자에게 노출 안 함) |
| 403 `edge-blocked` (허용 IP) | `CONFIG_ERROR` | "일시적인 오류가 발생했습니다." **원인을 노출하지 않는다** |
| 404 `stock-not-found` | `NOT_FOUND` | "해당 종목을 찾을 수 없습니다." |
| 429 `rate-limit-exceeded` | `BUSY` | "요청이 많아 잠시 지연되고 있습니다." |
| 500 `internal-error` / `maintenance` | `UPSTREAM_ERROR` | "시세 정보를 불러오지 못했습니다." |
| 타임아웃 (8초) | `TIMEOUT` | "응답이 지연되고 있습니다." |

> `CONFIG_ERROR`는 사용자에게 원인을 알리지 않되 **서버 로그·Sentry에는 명확히 남긴다.** 허용 IP 등록 누락은 배포 직후 가장 흔한 장애 원인이며, 빠르게 식별되어야 한다.

### 11.4 우리 API의 보호

Phase 1은 인증이 없으므로, 우리 엔드포인트가 무단으로 시세 프록시로 사용될 수 있다.

| 조치 | 내용 |
|---|---|
| Rate limiting | IP당 60 req/min (Fly.io 앞단 또는 미들웨어) |
| CORS | 자체 도메인만 허용 |
| 캐시 우선 | 캐시 히트 요청은 토스 API를 호출하지 않으므로 남용에도 상류 부담 없음 |
| 심볼 검증 | `stock` 테이블에 존재하는 심볼만 허용. 임의 문자열 통과 차단 |

---

## 12. Edge Case

### 12.1 데이터 관련

| 케이스 | 감지 | 동작 |
|---|---|---|
| **신규 상장 (봉 부족)** | `dataPoints < 최소 요구량` (§7.7) | 가능한 기간으로 자동 강등 + **"상장 후 N거래일치 데이터로 계산"** 안내. 40봉 미만이면 위치 자체 미표시 |
| **MA60 계산 불가 (61봉 미만)** | 배열 길이 | 화면 3 전체를 `INSUFFICIENT_DATA` 상태로. 위치(화면 2)는 정상 표시 |
| **최고가 = 최저가** (전 종가 동일) | `high == low` | **퍼센타일은 division by zero가 없다.** midrank로 50.0% 산출. 단 "가격 변동이 없어 위치 판단이 어렵습니다" 주석 부착 |
| **거래정지 / 정리매매** | `/stocks/{symbol}/warnings` | 위치·추세 계산을 **수행하지 않는다.** 배지 + "거래가 정지된 종목입니다" |
| **투자경고 / 투자위험** | 동일 | 계산은 수행하되 배지를 **최상단에 강조 표시** |
| **VI 발동 중** | 동일 | 현재가 옆 배지 |
| **상장폐지 예정** | `listing_status` | 검색 결과에서 제외하지 않되 배지 표시 |
| **액면분할 / 병합** | `adjusted=true` | 토스가 수정주가로 보정. **단 캐시된 과거 봉은 보정 전 값일 수 있음** → §12.2 |
| **급등락 (상한가/하한가)** | 등락률 절대값 | 정상 계산. 단 퍼센타일은 이상치에 강인하므로 Min-Max 대비 왜곡 적음 |
| **거래량 0인 날** | `volume == 0` | MA 계산에는 포함(종가는 존재), 거래량 확인 필터에서는 평균 산출 시 포함 |

### 12.2 액면분할 캐시 무효화 — 별도 주의

캔들 캐시는 다음 장 마감까지 유지되지만, **액면분할이 발생하면 과거 봉의 수정주가가 소급 변경된다.** 캐시된 과거 봉과 새로 받은 봉의 스케일이 달라져 MA와 퍼센타일이 모두 틀어진다.

```
감지 로직 (일 1회 배치):
  캐시된 최신 봉의 close 와, API에서 새로 받은 동일 날짜 봉의 close 를 비교
  | cached_close - fresh_close | / fresh_close > 0.005  (0.5%)
    → 해당 종목의 price_candle 전체 삭제 후 250봉 재수집
```

거래일 기준으로 동일한 날짜의 종가가 0.5% 이상 달라지는 것은 정상 상황에서 발생하지 않는다. 이 검사를 **매 배치마다 최신 1봉에 대해 수행**하는 것으로 액면분할·병합·대규모 배당 조정을 모두 잡을 수 있다.

### 12.3 시장 / 시간 관련

| 케이스 | 감지 | 동작 |
|---|---|---|
| **한국장 휴장** | `/market-calendar/KR` | KR 종목: 폴링 중단, "휴장일 — N월 N일 종가 기준" |
| **미국장 휴장** | `/market-calendar/US` | US 종목만 해당. 관심종목에 KR·US가 섞여 있으면 **종목별로 다른 상태**를 표시해야 함 |
| **장 시작 전 / 종료 후** | 캘린더 세션 시간 | "장 종료 · 종가 기준". 프리·애프터마켓 시세는 사용하지 않음 |
| **KR·US 혼재 관심종목** | 시장별 분기 | 각 행에 개별 기준 시각 표시. 리스트 상단 일괄 시각은 **표시하지 않는다** (오해 유발) |
| **서머타임 전환** | 캘린더 API | 하드코딩하지 않고 항상 캘린더 API 응답을 신뢰 |
| **미국장이 KST 자정을 가로지름** | 캘린더 3개 항목 전체 조회 | `today` 만 보면 안 된다 ↓ |

#### 12.3-a. 미국 정규장은 KST 날짜 경계를 가로지른다 (실측, 2026-08-28)

토스 캘린더의 `today` 는 **KST 날짜** 기준이다. 그런데 미국 정규장은 KST 22:30 에 시작해 **다음날** 05:00 에 끝난다. 그래서 세션 뒤쪽 5시간은 `today` 가 아니라 `previousBusinessDay` 에 적혀 있다.

실측 — 2026-08-28 00:49 KST (= 08-27 11:49 ET, NYSE 정규장 한복판):

| 항목 | 날짜 | 정규장 구간 |
|---|---|---|
| `today` | 2026-08-28 | 08-28 22:30 → 08-29 05:00 |
| `previousBusinessDay` | 2026-08-27 | 08-27 22:30 → **08-28 05:00** ← 지금 돌아가는 세션 |

`today` 만 보고 판정하면 **매 미국 세션의 약 77%(00:00~05:00 KST)를 장 마감으로 잘못 판정한다.** 표기만 틀리는 게 아니다 — `isRealtime` 이 false 로 내려가면서 §7.4 의 "장중에는 최신 봉 종가를 현재가로 대입" 분기가 통째로 어긋난다.

**대응.** `previousBusinessDay` · `today` · `nextBusinessDay` 세 항목의 정규장 구간을 모두 훑어, **지금 시각을 포함하는 세션이 하나라도 있으면 `OPEN`** 으로 판정한다 (`src/lib/market/calendar.ts`). 어느 날짜 항목에 적혀 있든 지금 돌아가는 세션이 사실이기 때문이다.

`referenceTradeDate` 도 같은 이유로 `today.date` 가 아니라 **실제로 열려 있는 세션의 날짜**를 쓴다. 회귀 테스트는 `tests/calendar.test.ts` 에 있다.

### 12.4 인프라 / API 장애

| 케이스 | 동작 |
|---|---|
| **토스 API 5xx / 타임아웃** | 캐시된 마지막 성공 데이터를 표시 + "N분 전 정보입니다" 배너. 캐시도 없으면 에러 화면 + 재시도 버튼 |
| **429 Rate Limit** | `Retry-After` 대기 후 지수 백오프 재시도(최대 3회). 사용자에게는 스켈레톤 유지 |
| **토큰 만료 (401)** | 자동 재발급 후 1회 재시도. 재실패 시 `UPSTREAM_ERROR` |
| **403 (허용 IP 미등록)** | `CONFIG_ERROR`. **Sentry 즉시 알림** — 배포 사고 가능성 |
| **관심종목 일부 실패** | 실패한 행만 "정보를 불러오지 못했습니다" 표시. **전체 화면을 에러로 만들지 않는다** |
| **검색 인덱스 로드 실패** | 검색 화면에 재시도 버튼. 관심종목 화면은 캐시된 종목명으로 정상 동작 |
| **DB 연결 실패** | 캐시 없이 토스 API 직접 호출로 폴백 (성능 저하, 기능은 유지) |

### 12.5 클라이언트

| 케이스 | 동작 |
|---|---|
| localStorage 용량 초과 / 비활성 | 관심종목 기능 비활성화 + 안내. 검색·조회는 정상 |
| 관심종목 20종목 초과 | 추가 차단 + "최대 20종목까지 등록할 수 있습니다" |
| 상장폐지된 관심종목 | 행에 "거래되지 않는 종목" 배지 + 삭제 유도 |
| 오프라인 | 마지막 성공 응답 표시 + 오프라인 배너 |
| 구간 경계 진동 | 히스테리시스로 방지 (§7.3) |

---

## 13. 투자정보 Disclaimer 및 표현 규정

> ⚠️ **이 절은 법률 자문이 아니다.** 아래는 위험을 낮추기 위한 제품 설계 지침이며, **서비스 출시 전 반드시 금융 규제 전문 법률 자문을 받아야 한다.** 특히 §13.4의 등록·신고 의무 여부는 확정적 판단이 필요한 사항이다.

### 13.1 서비스 포지션

POSTURE는 **공개된 시장 데이터를 가공해 표시하는 정보 제공 서비스**이며, 특정 종목의 매매를 권유하거나 투자 판단을 제공하지 않는다. 이 포지션은 다음 세 가지 설계 결정에 의해 뒷받침된다.

1. **주문 기능이 없다** (§8.3) — 앱 내에 매매 경로가 존재하지 않는다
2. **종합 판정을 만들지 않는다** (PP-05) — 위치와 추세를 조합한 결론을 앱이 대신 내리지 않는다
3. **모든 문구가 사실 진술이다** (PP-02) — 퍼센타일 방식 채택으로 문구가 검증 가능한 사실로 환원된다

### 13.2 표현 금지 목록 — CI 린트로 강제

**절대 사용 금지 (매매 권유·가치 판단)**

```
매수, 매도, 사다, 팔다, 사세요, 파세요, 진입, 청산, 익절, 손절,
추천, 유망, 기회, 타이밍, 적기, 좋은 위치, 나쁜 위치,
저평가, 고평가, 싸다, 비싸다, 저렴, 부담,
저점, 고점, 바닥, 천장, 무릎에서, 어깨에서,
목표가, 적정가, 전망, 예상, 예측, 상승할, 하락할,
주의, 위험 (투자 맥락에서 — 거래정지 배지 등 사실 표시는 예외)
```

**허용 (사실 진술)**

```
"최근 {N}거래일 중 약 {P}%의 날보다 높습니다"
"이 기간의 최고가는 {H}, 최저가는 {L}입니다"
"20일 평균 가격이 60일 평균 가격보다 높습니다 / 낮습니다"
"{D}거래일 전, 20일 평균선이 60일 평균선을 위로 / 아래로 지나갔습니다"
"그날의 거래량은 최근 20일 평균보다 적었습니다"
"거래가 정지된 종목입니다"
"상장 후 {N}거래일치 데이터로 계산했습니다"
```

> **"무릎"과 "어깨"는 구간 라벨로만 쓴다.** `무릎`(명사, 위치 라벨)은 허용하되, `무릎에서`(조사 결합, 매매 시점 암시)는 금지어에 포함한다. 속담 전체("무릎에서 사서 어깨에서 팔아라")는 앱 내 어디에도 등장시키지 않는다 — 마케팅 카피 포함.

### 13.3 화면별 고지 요구사항

| 위치 | 내용 | 형태 |
|---|---|---|
| **최초 실행 1회** | 서비스 성격 고지 + 확인 | 전체 화면 모달. 확인 없이 진행 불가. `disclaimerAckAt` 기록 |
| **모든 분석 화면 하단** | "본 정보는 투자 판단의 참고 자료이며, 투자 권유가 아닙니다. 투자의 최종 판단과 책임은 이용자 본인에게 있습니다." | 고정 바, 스크롤 무관 상시 노출 |
| **`ⓘ` 계산 근거 시트** | 계산식 / 기준 기간 / 데이터 출처(토스증권 Open API) / 데이터 시각 / 한계 | 바텀시트 |
| **`ⓘ` 시트 내 한계 고지** | "가격 범위 내 위치는 과거 데이터의 통계적 요약이며, 미래 가격을 예측하지 않습니다." | 강조 텍스트 |
| **`ⓘ` 시트 내 한계 고지 2** | "이동평균선 교차는 이미 발생한 가격 변화를 사후에 확인하는 지표입니다." | 강조 텍스트 |

**최초 실행 모달 문안 (초안)**

> POSTURE는 공개된 시세 데이터를 쉽게 볼 수 있도록 정리해 보여주는 서비스입니다.
>
> - 특정 종목의 매수·매도를 권유하지 않습니다.
> - 표시되는 위치와 흐름은 **과거 데이터를 요약한 것**이며, 앞으로의 가격을 예측하지 않습니다.
> - 시세는 실시간이 아닐 수 있으며, 실제 거래 가격과 다를 수 있습니다.
> - 투자의 최종 판단과 그 결과에 대한 책임은 이용자 본인에게 있습니다.
>
> 데이터 출처: 토스증권 Open API

### 13.4 법률 검토가 필요한 항목 (확정 판단 불가)

아래는 **반드시 전문가 확인이 필요한 사항**이며, 이 문서가 답을 제시할 수 없다.

| # | 검토 항목 |
|---|---|
| L-01 | 본 서비스가 자본시장법상 **투자자문업**에 해당하는지. 특히 "위치+추세를 특정 종목에 대해 표시"하는 행위의 성격 |
| L-02 | **유사투자자문업 신고** 의무 대상 여부 |
| L-03 | 무료 서비스와 유료화 시점의 규제 차이 |
| L-04 | 푸시 알림(F-LT-03) 도입 시 성격 변화 — **가장 위험도가 높은 기능** |
| L-05 | 시세 데이터 재배포에 대한 KRX·거래소의 별도 라이선스 요건 (토스 약관과 별개일 수 있음) |
| L-06 | 서비스명·마케팅 카피에 "무릎/어깨" 속담을 사용하는 것의 리스크 |
| L-07 | 면책 조항의 실효성 및 이용약관 필수 기재사항 |

> **L-05는 P-01(§0)과 함께 확인한다.** 토스증권 약관이 허용하더라도, 거래소 시세 데이터의 제3자 재배포에는 별도 조건이 붙는 경우가 있다.

---

## 14. 성공 지표

### 14.1 북극성 지표

**주간 활성 사용자 중 관심종목을 1개 이상 등록한 비율**

이 앱의 가치는 "한 번 검색해봤다"가 아니라 "내 종목을 이 척도로 계속 본다"에 있다. 관심종목 등록은 그 전환을 나타내는 가장 강한 신호다.

### 14.2 지표 체계

| 구분 | 지표 | 출시 4주 목표 | 측정 방법 |
|---|---|---|---|
| **이해** | 종목 상세 진입 후 **10초 이상 체류** 비율 | 60% | 즉시 이탈 = 이해 실패 |
| **이해** | `ⓘ` 계산 근거 시트 열람률 | 15% | 낮으면 문구가 이미 충분, 너무 높으면 문구가 불명확 |
| **이해** | 화면 4(쉬운 설명) 도달률 | 25% | 수치를 못 읽은 사용자의 안전망 작동 여부 |
| **핵심 기능** | **기간 토글 사용률** | 30% | 🔑 §5.3의 핵심 인터랙션. 낮으면 토글 존재를 모르는 것 |
| **핵심 기능** | 화면 3(흐름) 탭 전환율 | 45% | 추세 정보 수요 검증 |
| **전환** | 세션 내 관심종목 1개 이상 등록 | 20% | |
| **전환** | 관심종목 보유 사용자의 평균 등록 종목 수 | 5개 | |
| **재방문** | 7일 재방문율 | 25% | |
| **재방문** | 관심종목 등록자의 7일 재방문율 | **50%** | 등록이 재방문을 만드는지 검증 |
| **성능** | 종목 상세 LCP (캐시 히트) | < 1.5s | |
| **성능** | 종목 상세 LCP (캐시 미스) | < 3.0s | 캔들 2회 호출 포함 |
| **성능** | 기간 토글 전환 반영 | **< 100ms** | API 호출 0회이므로 달성 가능해야 함 |
| **품질** | 토스 API 에러율 | < 1% | |
| **품질** | 캔들 캐시 히트율 | > 85% | 낮으면 프리워밍 정책 조정 |
| **품질** | `INSUFFICIENT_DATA` 노출 비율 | < 5% | 높으면 최소 봉 수 기준 재검토 |

### 14.3 관찰 항목 (목표 없음, 학습 목적)

| 항목 | 알고 싶은 것 |
|---|---|
| 6구간 실제 분포 | 퍼센타일이 정말 균등하게 분포하는지. 특정 구간에 몰리면 §7.3 경계값 재조정 |
| 기간별 선택 비율 | 60/120/250 중 무엇이 실제로 쓰이는지. 기본값 재검토 근거 |
| 검색 → 이탈 지점 | 검색 결과에서 이탈하는지, 상세에서 이탈하는지 |
| 교차 배지 노출률 | 실제로 몇 %의 조회에서 교차 배지가 보이는지. §7.5 필터가 과도한지 |
| KR vs US 조회 비율 | 미국 종목 수요가 실제로 있는지 |

### 14.4 측정하지 않는 것

**"사용자가 이 앱을 보고 매매했는가"를 측정하지 않는다.** 이 지표를 추적하는 순간 제품 목표가 매매 유도로 이동하며, §13의 포지션 전체와 충돌한다. 앱 내에 매매 경로가 없으므로 측정 수단도 없다.

---

## 15. 개발 순서

### Phase 0 — 선행 조건 해소 (기간 미정, 외부 의존)

| # | 작업 | 산출물 |
|---|---|---|
| 0-1 | **토스증권 약관 문의** (P-01) | 공개 서비스 운영 가능 여부 서면 확인 |
| 0-2 | 토스증권 계좌 개설 → WTS에서 `client_id`/`client_secret` 발급 | 자격증명 |
| 0-3 | Fly.io 앱 생성 + **dedicated IPv4 확보** → 토스 WTS에 허용 IP 등록 | 고정 IP |
| 0-4 | ✅ **완료 (2026-08-27)** — API 응답 실측 | `npm run probe` · `docs/probe/` · §7.1 실측 반영 · §8.1-a |
| 0-5 | 법률 자문 착수 (§13.4) | 검토 의견서 |

> **0-4를 반드시 수행한다.** 특히 확인할 것: ① `/stocks/all`이 한글 종목명을 제공하는가 ② `before` 페이지네이션으로 250봉 이상 소급이 가능한가 ③ 미국 종목의 캔들 응답 타임존 처리. 이 중 하나라도 예상과 다르면 §7·§8이 수정되어야 한다.

### Phase 1 — MVP (약 4주)

| 단계 | 기간 | 범위 | 검증 기준 |
|---|---|---|---|
| **1-1 데이터 레이어** | 5일 | 토스 클라이언트 · 토큰 캐싱 · 토큰 버킷 큐 · 에러 매핑 · ~~Postgres~~ **SQLite** 스키마 · 캔들 캐시 · 마스터 동기화 배치 | **삼성전자·AAPL 250봉이 DB에 정확히 적재됨.** 토스 웹 차트와 종가 대조 일치 → `npm run sync:candles` 가 자동 검사 |
| **1-2 지표 엔진** | 4일 | 퍼센타일(midrank) · 구간 매핑 · SMA · 교차 감지 · 필터 · 템플릿 조립 · **단위 테스트** | 알려진 종목 5개에 대해 손계산 값과 일치. 엣지 케이스(전 종가 동일, 봉 부족) 테스트 통과 |
| **1-3 API 레이어** | 3일 | `/api/stock/{symbol}` · `/api/watchlist` · `/search-index.json`(정적) · 캐시 · Rate limiting | 응답 계약(§6.4) 준수. 캐시 히트 시 토스 호출 0회 확인 |
| **1-4 검색** | 3일 | ✅ **완료 (2026-08-28)** — 화면 1 · 자체 인덱스 · 초성 검색 · 최근 검색어 | ✅ "ㅅㅅㅈㅈ"·"samsung"·"005930"·"삼성" 모두 삼성전자 1위 (실데이터 15,262건, 0.5~11ms) |
| **1-5 종목 상세** | 6일 | ⏳ **코드 완료 · 캐릭터 에셋 대기 (2026-08-28)** — 화면 2·3·4 · 기간 토글 · MA 미니 차트 · `ⓘ` 시트 · 캐릭터 슬롯 | ✅ 기간 전환 3,000회 3.6ms (목표 100ms/회). PP-01 은 `CharacterArt` 가 인자를 받지 않는 구조로 강제 — D-01 에셋 수령 후 육안 검수 |
| **1-6 관심종목** | 3일 | 화면 5 · localStorage · 정렬 · 일괄 시세 | 20종목 조회 시 토스 호출 1회 확인 |
| **1-7 상태 처리** | 3일 | 데이터 부족 · 거래정지 · 휴장 · 에러 8종 · 로딩 스켈레톤 | §12 전 케이스 시나리오 테스트 |
| **1-8 법적 대응** | 2일 | Disclaimer · 최초 모달 · **금지어 린트 규칙** · 이용약관 | 린트가 금지어를 실제로 차단하는지 확인 |
| **1-9 폴리싱 & QA** | 3일 | 접근성 · 반응형 · 성능 · Sentry | Lighthouse 접근성 90+ |
| | **약 4주** | | |

> **1-1과 1-2 사이에 게이트를 둔다.** 실제 종목 데이터가 정확히 적재되고 토스 웹 화면과 대조 검증되기 전에는 지표 엔진을 시작하지 않는다. 데이터가 틀린 상태에서 계산 로직을 만들면 어느 쪽이 문제인지 분리할 수 없다.

### Phase 2 — 계정 및 맥락 (약 2.5주)

| 단계 | 기간 | 범위 |
|---|---|---|
| 2-1 | 4일 | Supabase Auth · 관심종목 서버 동기화 · 로컬↔서버 마이그레이션 |
| 2-2 | 3일 | 미니 스파크라인 (F-SPARK-01/02) |
| 2-3 | 2일 | 유의사항 상세 · 관심종목 프리워밍 배치 |
| 2-4 | 3일 | 접근성 강화 · 캐릭터 위치의 스크린리더 대체 텍스트 |
| 2-5 | 2일 | 지표 대시보드 · 6구간 실제 분포 분석 → 경계값 재조정 |

### Phase 3 — 확장 (범위 미확정)

Phase 1·2 지표를 확인한 뒤 착수 여부를 결정한다.

- MA50/200 장기 관점 토글
- 푸시 알림 (**L-04 법률 검토 통과가 전제**)
- 종목 뉴스 (별도 탭 완전 분리)
- 지수 대비 상대 위치

---

## 16. 미결정 사항

### 16.1 서비스명

`POSTURE`는 코드네임이다. 네이밍 시 다음 제약을 고려한다.

| 제약 | 내용 |
|---|---|
| **속담 사용 금지** | "무릎에서 사서 어깨에서 팔아라"를 서비스명·슬로건에 사용하지 않는다 (§13.2, L-06). 매매 권유로 해석될 여지가 가장 큰 지점 |
| 신체 은유 유지 | 앱의 핵심 정체성이므로 이름에 반영되면 좋음 |
| 중립성 | 가치 판단 단어("찬스", "타이밍", "저점") 배제 |
| 후보 방향 | 위치·좌표·눈금·자세 계열 / 신체 부위 중립 표현 계열 |

### 16.2 그 외

| # | 항목 | 결정 필요 시점 |
|---|---|---|
| D-01 | 캐릭터 비주얼 디자인 (성별 중립·연령 중립 필요) | ⏳ **에셋 대기** — 기획자가 이미지 제공 예정 ↓ |
| D-02 | 라이트/다크 UI 방향 | ✅ **결정: 라이트 고정** ↓ |
| D-03 | DB를 Postgres로 갈지 SQLite로 갈지 | ✅ **결정: SQLite** (`src/lib/db/`) |
| D-04 | 검색 인덱스를 클라이언트 전량 로드할지 서버 검색 API로 할지 | ✅ **결정: 전량 클라이언트 로드** ↓ |
| D-05 | PWA 설치 유도 여부 | Phase 1-9 |

#### D-01 처리 방침 (2026-08-28)

캐릭터 아트는 기획자가 이미지로 제공한다. 그동안 1-5 가 멈추지 않도록 **캐릭터를 하나의 교체 가능한 슬롯으로 격리**한다.

| 규칙 | 이유 |
|---|---|
| 캐릭터는 `Character` 컴포넌트 하나로만 들어온다 | 에셋이 오면 **이 파일 하나만** 갈아끼우면 된다 |
| 부위별 세로 좌표를 `BODY_ANCHORS` 상수로 분리 | 마커 정렬이 아트에 종속되지 않는다. 새 아트의 비율에 맞춰 숫자만 고친다 |
| 위치 마커·구간 라벨은 캐릭터 **바깥** 레이어 | PP-01(표정·색상 불변)을 아트가 어떻게 생겼든 구조적으로 강제한다 |

**에셋 수령 시 확인할 것** — PP-01·D-01 이 요구하는 성질은 아트에 내장돼야 한다.

- 정면 전신 · 중립 표정 **한 컷**일 것. 위치별 변형(웃는 얼굴/찡그린 얼굴)이 딸려 오면 쓰지 않는다 — 그것을 쓰는 순간 캐릭터가 평가자가 된다.
- 성별·연령을 특정하는 단서(머리 모양, 복장, 체형)가 약할 것.
- 배경 투명 · 벡터(SVG) 우선. 래스터라면 세로 해상도 1,200px 이상.

#### D-02 판단 근거 (2026-08-28)

**라이트 고정.** 다크 팔레트를 만들지 않는다.

색을 전부 CSS 변수로 빼둔 구조(`globals.css`)는 그대로 둔다. 나중에 다크를 넣기로 하면 그 블록만 추가하면 되고, 지금 당장은 검수 조합이 절반이라 1-9 QA 가 가벼워진다. 특히 **등락률 색(상승 빨강/하락 파랑)은 §5.1 이 인정한 유일한 의미색**이라 배경이 둘이면 두 배경 모두에서 대비를 따로 맞춰야 하는데, 그 비용을 Phase 1 에서 치를 이유가 없다.

#### D-04 판단 근거 (실측, 2026-08-27)

전체 상장 **15,272종목**을 실제로 직렬화해 측정한 값이다. §15 Phase 0-4가 "종목 수 실측 후"라고 미뤄둔 그 수치다.

| 구성 | 원본 | gzip | brotli |
|---|---|---|---|
| A) 전 필드 객체 배열 | 2,205KB | 372KB | 283KB |
| B) 튜플 (키 제거, 전체) | 1,190KB | **336KB** | **265KB** |
| C) 국내만, 튜플 | 320KB | 99KB | 79KB |
| D) 한글명 보유분만, 튜플 | 634KB | 169KB | 138KB |

키 이름을 없앤 튜플 형태가 원본을 절반으로 줄이지만, 압축 후 차이는 372KB → 336KB로 크지 않다. 압축기가 반복되는 키를 이미 잘 지운다.

**쟁점.** 전량 로드(B)는 검색이 네트워크 0회·오프라인 동작·초성 즉시 반응이 되는 대신 첫 진입에 265~336KB를 받는다. 서버 검색 API는 첫 진입이 가볍지만 타이핑마다 왕복이 생기고 §11.4 rate limiting 대상이 하나 늘어난다.

**결정 — 전량 클라이언트 로드 (B 형태).**

한글 IME는 한 글자를 완성하는 동안에도 중간 상태를 계속 만들어낸다. 여기에 서버 왕복이 붙으면 디바운스를 얼마로 잡든 "입력이 밀리는" 느낌이 남는다. 검색은 화면 1이자 이 앱의 첫인상이고, 타깃이 초보자다. **265KB 한 번**과 **모든 키 입력의 지연** 중에서는 전자가 낫다.

부수적으로 코드가 줄어든다. 서버 검색을 택하면 디바운스·요청 취소·로딩/빈결과/에러 상태·rate limiting을 화면 1에 얹어야 하지만, 로컬 인덱스는 fetch 한 번과 순수 필터 함수가 전부다.

| 항목 | 확정 |
|---|---|
| 경로 | `GET /search-index.json` — **API 라우트가 아니라 정적 산출물**이다 (아래 실측) |
| 생성 | 마스터 동기화 배치가 `public/search-index.json` 으로 굽는다 (`npm run build:index`) |
| 형식 | 튜플 배열 `[symbol, nameKo, nameEn, initials, market]` |
| 대상 | `listing_status = 'LISTED'` 전량 (15,272종목) |
| 압축 | gzip (Next 정적 핸들러). 실측 전송 **346KB** |
| 캐시 | `Cache-Control: public, max-age=86400, stale-while-revalidate=604800` + ETag·Last-Modified |
| 클라이언트 캐시 | 이미 의존성에 있는 `@tanstack/query-sync-storage-persister` 재사용 |
| 로딩 | 첫 페인트를 막지 않는다. 검색창은 즉시 그리고 인덱스는 뒤에서 받는다 |

**⚠️ 왜 API 라우트가 아닌가 (실측 정정).**

처음에는 `GET /api/search-index` 라우트 핸들러로 만들었다. 그런데 **`next start` 는 라우트 핸들러 응답을 압축하지 않는다.** 페이지 HTML 과 `public/` 정적 파일에는 gzip 이 걸리는데, Route Handler 응답에는 걸리지 않는다. 응답에 `Content-Encoding` 을 직접 붙여도 Next 가 지운다 (`Vary` 도 자체 값으로 덮어쓴다).

| 경로 | 전송 크기 | Content-Encoding |
|---|---|---|
| `GET /api/search-index` | 1,218,732 B | (없음) |
| `GET /search-index.json` | **346,551 B** | `gzip` |

D-04 는 265~336KB 를 전제로 전량 로드를 택한 결정이다. 압축이 빠지면 1.19MB 가 되어 근거가 **4.5배** 어긋난다. 그래서 인덱스는 배치 산출물로 바꿨다.

부수 효과로 §11.4 가 더 깨끗해진다 — 정적 파일이라 상류 부담이 0이고 rate limiting 대상에서 빠진다.

> **남은 것:** brotli(262KB)는 gzip(346KB)보다 84KB 작지만 Next 정적 핸들러가 gzip 만 한다. 얻으려면 앞단(Fly.io 프록시)이나 커스텀 서버가 필요하다. 84KB 차이라 Phase 1 을 막을 이유는 아니며, 배포 설정 과제로 넘긴다.
>
> 같은 이유로 `/api/stock/{symbol}`·`/api/watchlist` 응답도 압축되지 않는다. 종목 상세가 수십 KB 수준이라 Phase 1 에서는 견딜 만하지만, 압축을 켜면 그대로 이득이다.

## 부록 A. 참조한 토스증권 공식 문서

| 문서 | URL |
|---|---|
| 개발자센터 | `https://developers.tossinvest.com/docs` |
| LLM 가이드 | `https://developers.tossinvest.com/llms.txt` |
| 개요 (Rate Limit · 에러 · WebSocket) | `https://openapi.tossinvest.com/openapi-docs/overview.md` |
| API 레퍼런스 (Markdown) | `https://openapi.tossinvest.com/openapi-docs/latest/api-reference/README.md` |
| OpenAPI JSON (source of truth) | `https://openapi.tossinvest.com/openapi-docs/latest/openapi.json` |
| AsyncAPI JSON (WebSocket) | `https://openapi.tossinvest.com/openapi-docs/latest/asyncapi.json` |

> API 스펙은 변경될 수 있다. **Phase 0-4 실측 시 `openapi.json`을 내려받아 저장소에 스냅샷으로 보관**하고, 스펙 변경을 감지할 수 있게 한다.

---

*문서 끝. v1.0 — 2026-08-26*
